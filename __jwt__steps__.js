/*** *  
 * Install jwt-------json web token
 * requires ----- mane import k requires bola hoy------
 * install cookine parser -------------- and require------------
 * user login and create a token 
 * 
 * jwt.sign(data, secret, {expiresIn: '5h or etc.......})
 * 
 * set token to the cookie of res.cookie('token', token, {
 * 
 *   httpOnly: true,
 *   secure: false
 * }).send({})
 * 
 * cors({
 * origin: ['font end a j api thke data asbe sei link ta ekhabe bosai te hobe'],
 * credentials: true,
 * 
 * })
 * 
 * .......... Clint site ja asbe ....................
 * 
 * client: {
 *  withCredentials: true
 * }
 * 
 * 
 */


